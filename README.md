# Sysproxy

## Usage
```
% ./sysproxy -help
Usage of ./sysproxy:
  -bypass string
        bypass join by ,
  -http string
        http proxy server and port
  -https string
        https proxy server and port
  -show
        show all proxies
  -socks string
        socks proxy server and port
  -stop
        disable all proxies
```